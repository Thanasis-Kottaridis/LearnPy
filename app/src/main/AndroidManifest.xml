<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unipi.kottarido.learnpy.learnpy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowStatisticsActivity"></activity>
        <activity android:name=".UpdateTodaysChallActivity" />
        <activity android:name=".AddAdminActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".PracticeActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".AuthenticationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntroActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SetUpProfileActivity" />
    </application>

</manifest>